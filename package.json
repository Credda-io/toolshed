{
  "name": "toolshed",
  "version": "0.1.0",
  "private": true,
  "description": "A deliberately bug-seeded community tool-lending library. Demo corpus for Credda.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run --dir test",
    "repro": "vitest run --dir repro",
    "negative": "vitest run --dir negative",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vitest": "^3.2.7"
  }
}
