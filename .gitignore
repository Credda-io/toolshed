node_modules/
dist/
*.tsbuildinfo
coverage/

# Never commit local environment files. A GOOGLE_CLIENT_SECRET reached this
# tree in cleartext on 2026-05-02 because a repo had no rule like this one,
# and removing it three commits later did not unpublish the blob.
.env
.env.*
!.env.example
